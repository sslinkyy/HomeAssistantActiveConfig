template:
  - sensor:
      - name: Lights On
        unique_id: lights_on
        state: >-
          {{ states.light
             | selectattr('state','eq','on')
             | list | count }}
      - name: Open Doors Windows
        unique_id: open_doors_windows
        state: >-
          {{ states.binary_sensor
             | selectattr('attributes.device_class','in',['door','window'])
             | selectattr('state','eq','on')
             | list | count }}
      - name: People Home
        unique_id: people_home
        state: >-
          {{ states.person
             | selectattr('state','eq','home')
             | list | count }}
      - name: Updates Available
        unique_id: updates_available_count
        state: >-
          {{ states.update | selectattr('state','eq','on') | list | count }}
      - name: Unavailable Entities
        unique_id: unavailable_entities_count
        state: >-
          {{ states | selectattr('state','eq','unavailable') | list | count }}
      - name: Active Media Players
        unique_id: active_media_players
        state: >-
          {{ states.media_player
             | selectattr('state','in',['playing','buffering','paused'])
             | list | count }}
